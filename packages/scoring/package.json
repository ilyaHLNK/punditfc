{
  "name": "@punditfc/scoring",
  "version": "0.0.0",
  "private": true,
  "description": "Pure scoring engine: points for a bet, with no I/O of any kind",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@punditfc/contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
